<!DOCTYPE html>
<html>
<head>
	<title>Lab App</title>
	<% include partials/frameworks %>
</head>
<body>
	<div class="container">
	<h1>Hello</h1>
		<% for (var i in results) { %>
			<div class="row">
				<%= results[i].studentName %>
				<%= results[i].teacherName %>
				<%= results[i].score %>
				<%= results[i].labName %>
				<%= results[i].date %>
				<%= results[i].time %>
			</div>
		<% } %>
</body>
</html>